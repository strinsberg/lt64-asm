(lt64-asm
  (static
    (:str hello "Hello, World!"))  ;; store null terminated str labeled hello

  (instruct
    (:print-str hello)             ;; print string at hello
    (:halt)))

  ;; could also be (:push hello) (:print-str) (:halt)
